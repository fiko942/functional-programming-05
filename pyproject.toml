[tool.poetry]
name = "golden-dragon-wok"
version = "0.1.0"
description = "CLI ERP for Golden Dragon Wok - functional style"
authors = ["Generated"]

[tool.poetry.dependencies]
python = ">=3.10"

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
